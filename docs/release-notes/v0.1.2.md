# Release Notes - v0.1.2

**Release Date**: 2025-11-19

## Features
- Add refresh keybinding ('r') to sprint board

## Updates

### Technical
- FirstTimeError struct: remove unwanted `fmt.Sprint()` from Error function
- Create path variables for `logs` folder and `credentials` file in `config` package
